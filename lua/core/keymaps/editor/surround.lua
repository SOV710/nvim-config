-- Keymaps managed by mini.surround via its `mappings` config.
-- Listed here for which-key registration / documentation purposes.
return {
  { 'sa', desc = 'Add surrounding', mode = { 'n', 'v' }, remap = false },
  { 'sd', desc = 'Delete surrounding', remap = false },
  { 'sr', desc = 'Replace surrounding', remap = false },
  { 'sf', desc = 'Find surrounding (right)', remap = false },
  { 'sF', desc = 'Find surrounding (left)', remap = false },
  { 'sh', desc = 'Highlight surrounding', remap = false },
}
